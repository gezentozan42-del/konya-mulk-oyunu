(()=>{
  if(window.__konyaFxLoadedV3)return; window.__konyaFxLoadedV3=true;
  const PIPS={1:[5],2:[1,9],3:[1,5,9],4:[1,3,7,9],5:[1,3,5,7,9],6:[1,3,4,6,7,9]};
  const TYPE_ICON={property:'🏠',station:'🚉',utility:'💡',chance:'❓',chest:'🎁',tax:'💸',jail:'🔒',gotojail:'🚓',free:'☕',start:'🏁'};
  const tokens=new Map(); let prev=null,rollTimer=null,animSeq=0,audioCtx=null;
  const sleep=ms=>new Promise(r=>setTimeout(r,ms));
  function beep(freq=440,dur=.05,vol=.02){try{audioCtx=audioCtx||new (window.AudioContext||window.webkitAudioContext)();const o=audioCtx.createOscillator(),g=audioCtx.createGain();o.frequency.value=freq;o.type='sine';g.gain.value=vol;o.connect(g);g.connect(audioCtx.destination);o.start();g.gain.exponentialRampToValueAtTime(.001,audioCtx.currentTime+dur);o.stop(audioCtx.currentTime+dur)}catch(e){}}
  function escFx(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
  function toast(title,text){let l=document.getElementById('fxToastLayer');if(!l){l=document.createElement('div');l.id='fxToastLayer';document.body.appendChild(l)}const d=document.createElement('div');d.className='fx-toast';d.innerHTML=`<strong>${escFx(title)}</strong><span>${escFx(text)}</span>`;l.appendChild(d);setTimeout(()=>d.classList.add('fx-out'),2600);setTimeout(()=>d.remove(),2900)}
  function playerById(id,s=state){return s?.players?.find(p=>p.id===id)}
  function tileEl(i){return document.querySelectorAll('#board .tile')[i]||null}
  function ensureDice(){const old=document.getElementById('dice');if(!old)return null;if(!document.getElementById('fxDie1')){old.innerHTML='<div class="fx-dice-wrap"><div class="fx-die" id="fxDie1"></div><div class="fx-die" id="fxDie2"></div></div>';['fxDie1','fxDie2'].forEach(id=>{const e=document.getElementById(id);for(let i=1;i<=9;i++){const p=document.createElement('span');p.className='fx-pip';p.dataset.i=i;e.appendChild(p)}})}return [document.getElementById('fxDie1'),document.getElementById('fxDie2')]}
  function face(el,n){if(!el)return;el.querySelectorAll('.fx-pip').forEach(p=>p.style.opacity='0');(PIPS[n]||[]).forEach(i=>{const p=el.querySelector(`.fx-pip[data-i="${i}"]`);if(p)p.style.opacity='1'})}
  function startRoll(){const ds=ensureDice();if(!ds)return;clearInterval(rollTimer);ds.forEach(d=>{d.classList.add('fx-rolling');d.classList.remove('fx-settle')});rollTimer=setInterval(()=>{face(ds[0],1+Math.floor(Math.random()*6));face(ds[1],1+Math.floor(Math.random()*6))},75);beep(160,.08,.016)}
  function stopRoll(vals){const ds=ensureDice();if(!ds)return;clearInterval(rollTimer);rollTimer=null;ds.forEach(d=>d.classList.remove('fx-rolling'));face(ds[0],vals?.[0]||1);face(ds[1],vals?.[1]||1);ds.forEach(d=>{d.classList.remove('fx-settle');void d.offsetWidth;d.classList.add('fx-settle')});}
  function ensureLayer(){const b=document.getElementById('board');if(!b)return null;let l=document.getElementById('fxTokenLayer');if(!l){l=document.createElement('div');l.id='fxTokenLayer';b.appendChild(l)}return l}
  function ensureTokens(s){const layer=ensureLayer();if(!layer||!s)return;const ids=new Set(s.players.map(p=>p.id));for(const [id,e]of tokens){if(!ids.has(id)){e.remove();tokens.delete(id)}}s.players.forEach(p=>{let e=tokens.get(p.id);if(!e){e=document.createElement('div');e.innerHTML=`<span class="fx-token-letter">${escFx(p.name.slice(0,1).toUpperCase())}</span><span class="fx-token-label">${escFx(p.name)}</span>`;layer.appendChild(e);tokens.set(p.id,e)}e.className=`fx-token p${p.color}${s.turnPlayerId===p.id&&s.started?' fx-active':''}`;e.style.display=p.bankrupt?'none':'block'})}
  function groups(players){const g={};players.filter(p=>!p.bankrupt).forEach(p=>(g[p.pos]||(g[p.pos]=[])).push(p.id));return g}
  function offsets(n){return({1:[[0,0]],2:[[-14,-12],[14,12]],3:[[0,-15],[-14,11],[14,11]],4:[[-14,-14],[14,-14],[-14,14],[14,14]],5:[[0,0],[-16,-16],[16,-16],[-16,16],[16,16]]}[n]||[[0,0]])}
  function posFor(i,idx=0,count=1){const b=document.getElementById('board'),t=tileEl(i);if(!b||!t)return{x:20,y:20};const br=b.getBoundingClientRect(),r=t.getBoundingClientRect(),o=offsets(count)[idx]||[0,0];return{x:r.left-br.left+r.width/2+o[0],y:r.top-br.top+r.height/2+o[1]}}
  function place(players){const g=groups(players);players.filter(p=>!p.bankrupt).forEach(p=>{const list=g[p.pos]||[p.id],pt=posFor(p.pos,list.indexOf(p.id),list.length),e=tokens.get(p.id);if(e){e.style.left=pt.x+'px';e.style.top=pt.y+'px'}})}
  function route(a,b,newS,np){if(a==null||b==null||a===b)return[b];const total=(newS?.lastRoll?.[0]||0)+(newS?.lastRoll?.[1]||0);if(newS?.turnPlayerId===np.id&&total>0&&((a+total)%40)===b)return Array.from({length:total},(_,k)=>(a+k+1)%40);const d=(b-a+40)%40;if(d>0&&d<=12)return Array.from({length:d},(_,k)=>(a+k+1)%40);return[b]}
  async function animate(oldS,newS){const seq=++animSeq;ensureTokens(newS);if(!oldS){place(newS.players);return}const moved=newS.players.filter(np=>{const op=oldS.players.find(p=>p.id===np.id);return op&&op.pos!==np.pos&&!np.bankrupt});if(!moved.length){place(newS.players);return}const staged=JSON.parse(JSON.stringify(newS.players));moved.forEach(np=>{const op=oldS.players.find(p=>p.id===np.id),sp=staged.find(p=>p.id===np.id);if(op&&sp)sp.pos=op.pos});place(staged);await sleep(40);let max=0;const paths=new Map();moved.forEach(np=>{const op=oldS.players.find(p=>p.id===np.id),p=route(op?.pos,np.pos,newS,np);paths.set(np.id,p);max=Math.max(max,p.length)});for(let k=0;k<max;k++){if(seq!==animSeq)return;moved.forEach(np=>{const p=paths.get(np.id),sp=staged.find(x=>x.id===np.id);if(sp&&p[k]!=null)sp.pos=p[k]});place(staged);beep(230+k*12,.03,.009);await sleep(235)}if(seq===animSeq)place(newS.players)}
  function decorateTiles(s){const tiles=[...document.querySelectorAll('#board .tile')];tiles.forEach((t,i)=>{t.querySelectorAll('.fx-owner-mark,.fx-type-icon').forEach(x=>x.remove());t.classList.remove('fx-buyable');const bt=s.board?.[i],a=s.assets?.[String(i)];if(bt){const ic=document.createElement('span');ic.className='fx-type-icon';ic.textContent=TYPE_ICON[bt.type]||'';t.appendChild(ic)}if(a){const p=playerById(a.ownerId,s);if(p){const m=document.createElement('span');m.className=`fx-owner-mark p${p.color}`;m.textContent=p.name.slice(0,1).toUpperCase();m.title=`Sahibi: ${p.name}`;t.appendChild(m)}}});const me=s.players.find(p=>p.id===myPlayerId);if(me&&s.turnPlayerId===me.id&&s.lastRoll){const bt=s.board?.[me.pos],a=s.assets?.[String(me.pos)],t=tiles[me.pos];if(t&&bt&&['property','station','utility'].includes(bt.type)&&!a)t.classList.add('fx-buyable')}}
  function syncActions(s){const me=s.players?.find(p=>p.id===myPlayerId),myTurn=!!(s.started&&me&&!me.bankrupt&&s.turnPlayerId===me.id);const tile=me?s.board?.[me.pos]:null,a=me?s.assets?.[String(me.pos)]:null,buyable=tile&&['property','station','utility'].includes(tile.type);const roll=document.getElementById('rollBtn'),end=document.getElementById('endBtn'),buy=document.getElementById('buyBtn'),auc=document.getElementById('auctionBtn');if(roll){roll.disabled=!myTurn||!!s.lastRoll||!!s.auction;roll.classList.toggle('fx-pulse',!roll.disabled)}if(end){end.disabled=!myTurn||!s.lastRoll||!!s.auction;end.textContent=s.extraTurn&&myTurn&&s.lastRoll?'🎲 Çift attın • tekrar zar':'Sırayı bitir';end.classList.toggle('fx-pulse',!end.disabled)}if(buy){const can=myTurn&&!!s.lastRoll&&!s.auction&&buyable&&!a&&me.money>=tile.price;buy.classList.toggle('hidden',!can);buy.disabled=!can;buy.classList.toggle('fx-pulse',can)}if(auc){const can=myTurn&&!!s.lastRoll&&!s.auction&&buyable&&!a;auc.classList.toggle('hidden',!can);auc.disabled=!can}}
  function process(s){ensureDice();decorateTiles(s);syncActions(s);ensureTokens(s);animate(prev,s);if(s.lastRoll)stopRoll(s.lastRoll);else{const ds=ensureDice();if(ds&&!rollTimer){face(ds[0],1);face(ds[1],1)}}const oldLen=prev?.log?.length||0,newLen=s.log?.length||0;if(prev&&newLen>oldLen){const it=s.log[newLen-1],msg=it?.msg||it;if(msg){toast('Oyun bildirimi',msg);if(/satın|aldı|kazandı|ödedi|hap/i.test(msg))beep(630,.06,.015)}}prev=JSON.parse(JSON.stringify(s))}
  document.addEventListener('click',e=>{if(e.target?.id==='rollBtn')startRoll();if(e.target?.id==='buyBtn')beep(700,.08,.018)},true);
  window.addEventListener('resize',()=>state&&place(state.players));
  if(typeof socket!=='undefined')socket.on('state',s=>setTimeout(()=>process(s),0));
  const boot=setInterval(()=>{if(typeof state!=='undefined'&&state){clearInterval(boot);process(state)}},250);
})();
